# BSD_2_clause

#' Return a table of parts of speech
#'
#' Knowing the distribution of a word's part of speech (POS) can be an
#' informative characteristic for correcting errors. Note that this can be
#' computationally expensive.
#'
#' @param txt A character representation of a text
#' @return A table of word, POS, and frequency of the combination
#' @export
#' @examples
#' # get_POS(test_txt)
get_POS <- function(txt) {
  # return a table of word, POS, freq
}
